// Re-export — definição canônica em common/dtos (quebra do ciclo Auth↔Workspaces).
export {
  WorkspaceResponseDto,
  WorkspaceSeatsResponseDto,
} from '../../../common/dtos/workspace-response.dto';
